# Швидкий старт

## 1. Перевірка системи
```bash
python test_setup.py
```

## 2. Запуск бота
```bash
python main.py
```
або подвійний клік на `start.bat`

## 3. Керування

### Telegram
Відкрий бота @kurwapacurbot у Telegram:
- `/menu` — показати меню
- `/status` — перевірити статус
- Кнопки "Запустити" / "Зупинити" для керування пошуком

### Гарячі клавіші
- `F6` — пауза/продовження
- `F7` — вихід
- `F8` — запустити пошук
- `F9` — зупинити пошук

## Налаштування

Всі параметри у файлі `.env`:

```env
# Швидкість сканування (секунди)
SCAN_INTERVAL=0.30

# Пороги розпізнавання (0.0-1.0)
CONFIDENCE_ACCEPT=0.80
CONFIDENCE_SEARCHING=0.70
CONFIDENCE_SEARCH_BTN=0.70
CONFIDENCE_STOP_BTN=0.75
```

## Логи

Логи зберігаються у `logs/dota_helper_YYYYMMDD.log`

## Проблеми?

1. **Бот не знаходить кнопки**
   - Зменш `CONFIDENCE_*` у `.env` (наприклад, до 0.65)
   - Перевір, чи Dota 2 у вікнованому режимі

2. **Telegram не відповідає**
   - Перевір токен у `.env`
   - Запусти `python test_setup.py`

3. **Повільна реакція**
   - Зменш `SCAN_INTERVAL` до 0.20
   - Закрий інші програми
